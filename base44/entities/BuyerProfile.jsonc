{
  "name": "BuyerProfile",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "full_name": {
      "type": "string"
    },
    "address": {
      "type": "string"
    },
    "zip": {
      "type": "string"
    },
    "state": {
      "type": "string",
      "description": "US state resolved from geocoding, used for cross-state booking restrictions"
    },
    "latitude": {
      "type": "number",
      "description": "Geocoded latitude of the buyer's address"
    },
    "longitude": {
      "type": "number",
      "description": "Geocoded longitude of the buyer's address"
    },
    "resolved_city": {
      "type": "string",
      "description": "City resolved from geocoding"
    },
    "id_verification_status": {
      "type": "string",
      "enum": [
        "pending",
        "verified",
        "failed"
      ],
      "default": "pending",
      "rls": {
        "write": {
          "user_condition": {
            "role": "admin"
          }
        }
      }
    },
    "referral_code": {
      "type": "string",
      "description": "This neighbor's shareable referral code"
    },
    "referral_credit": {
      "type": "number",
      "default": 0,
      "description": "Credit balance auto-applied to the next booking"
    }
  },
  "required": [
    "user_id"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}