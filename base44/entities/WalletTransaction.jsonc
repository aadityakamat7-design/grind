{
  "name": "WalletTransaction",
  "type": "object",
  "properties": {
    "teen_user_id": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "earning",
        "cashout"
      ]
    },
    "amount": {
      "type": "number"
    },
    "status": {
      "type": "string",
      "enum": [
        "completed",
        "processing"
      ],
      "default": "completed",
      "description": "processing = cash-out submitted but funds haven't settled yet (24-48 hour window)"
    },
    "description": {
      "type": "string"
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "teen_user_id",
    "type",
    "amount"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.teen_user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}